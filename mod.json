{
	"geode": "5.6.1",
	"gd": {
		"win": "2.2081",
		"android": "2.2081"
	},
	"id": "le10x.double_click_exit",
	"version": "1.1.0",
	"name": "Double Click Exit",
	"developer": "le10x",
	"description": "Prevents accidental exits by requiring a double click on the exit button.",
	"settings": {
		"enable-double-click": {
			"name": "Enable Double Click",
			"description": "Requires two clicks to trigger the exit action.",
			"type": "bool",
			"default": true
		},
		"show-notification": {
			"name": "Show Notification",
			"description": "Show a message on the first click.",
			"type": "bool",
			"default": true
		},
		"custom-text": {
			"name": "Custom Message",
			"description": "Text shown when clicking once.",
			"type": "string",
			"default": "Click again to exit"
		},
		"click-speed": {
			"name": "Click Speed (ms)",
			"description": "Max time between clicks (0-1000ms).",
			"type": "int",
			"default": 500,
			"min": 0,
			"max": 1000,
			"control": {
				"slider": true
			}
		}
	}
}
